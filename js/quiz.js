"use strict";


$(document).ready(function() {

// This is found in the "robotJsonAccess.js"
  RobotBuilds.MainLoadBuildPromise();

  // let robotPlayer = {};

  // robotPlayer = new RobotBuilds.Players.Avian({name: "Dude", model: "Rotors"});
  // robotPlayer.setHealth();
  // robotPlayer.setModification(new RobotBuilds.Modification.Engine());
  // RobotBuilds.setPlayers(robotPlayer);

  // console.log(robotPlayer);

  // robotPlayer = new RobotBuilds.Players.Wheeled({name: "Dude2", model: "Standard Wheeled"});
  // robotPlayer.setHealth();
  // robotPlayer.setModification(new RobotBuilds.Modification.Armor());
  // RobotBuilds.setPlayers(robotPlayer);

  // console.log(robotPlayer);

  // Syntax to get an individual player
  // RobotBuilds.getPlayers()[Object.keys(RobotBuilds.getPlayers())[0]];

});